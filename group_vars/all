---
host_ipaddress: "{{ hostvars[inventory_hostname]['ansible_' + interface_name ]['ipv4']['address'] }}"
interface_name: "{{ ansible_interfaces[1] }}" # Update the array value according to the interafce count in facts output
